---
phase: 02-wht-tax-invoices-filing-deadlines
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - app/(app)/apps/wht-report/manifest.ts
  - app/(app)/apps/wht-report/page.tsx
  - app/(app)/apps/wht-report/actions.ts
  - app/(app)/apps/wht-report/components/wht-report-client.tsx
  - app/(app)/apps/wht-report/components/report-preview.tsx
  - app/(app)/apps/wht-report/components/pnd3-pdf.tsx
  - app/(app)/apps/wht-report/components/pnd53-pdf.tsx
  - app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx
  - exports/pdf/thai-pdf-styles.ts
autonomous: true
requirements: [WHT-03, WHT-04, WHT-05, INV-02]

must_haves:
  truths:
    - "50 Tawi certificate generates as 3-page PDF with all required fields per Section 50 Bis"
    - "PND3 report queries transactions with whtType=pnd3 and generates correct monthly summary"
    - "PND53 report queries transactions with whtType=pnd53 and generates correct monthly summary"
    - "Batch 50 Tawi generates all certificates for a month as ZIP download"
    - "Sequential document numbering for 50 Tawi certificates uses atomic counter"
    - "Report generation follows same one-click UX as PP30 from Phase 1"
  artifacts:
    - path: "app/(app)/apps/wht-report/page.tsx"
      provides: "WHT report page with month selector"
      min_lines: 20
    - path: "app/(app)/apps/wht-report/actions.ts"
      provides: "Server actions for WHT report data and 50 Tawi generation"
      exports: ["generateWHTReportAction", "generate50TawiAction"]
    - path: "app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx"
      provides: "50 Tawi PDF component with 3 copies"
      min_lines: 80
    - path: "app/(app)/apps/wht-report/components/pnd3-pdf.tsx"
      provides: "PND3 monthly WHT report PDF"
      min_lines: 50
    - path: "app/(app)/apps/wht-report/components/pnd53-pdf.tsx"
      provides: "PND53 monthly WHT report PDF"
      min_lines: 50
  key_links:
    - from: "app/(app)/apps/wht-report/actions.ts"
      to: "prisma.transaction"
      via: "Query transactions filtered by whtType and date range"
      pattern: "prisma\\.transaction\\.findMany"
    - from: "app/(app)/apps/wht-report/components/report-preview.tsx"
      to: "fifty-tawi-pdf.tsx, pnd3-pdf.tsx, pnd53-pdf.tsx"
      via: "Client-side pdf() generation from @react-pdf/renderer"
      pattern: "pdf\\("
    - from: "app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx"
      to: "exports/pdf/fonts.ts"
      via: "registerThaiFonts() for THSarabunNew"
      pattern: "registerThaiFonts"
---

<objective>
50 Tawi certificate PDF, PND3/PND53 monthly WHT report generation, and batch download.

Purpose: Enable users to generate WHT certificates (50 Tawi) for individual transactions or batch-monthly, and produce PND3/PND53 monthly filing reports -- the core WHT document generation. Follows the exact same one-click pattern as PP30 (per D-04).

Output: WHT report app at /apps/wht-report with month/year selector, 50 Tawi PDF (3 copies), PND3 PDF, PND53 PDF, report preview with summary, individual + batch ZIP download.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-wht-tax-invoices-filing-deadlines/02-CONTEXT.md
@.planning/phases/02-wht-tax-invoices-filing-deadlines/02-RESEARCH.md
@.planning/research/THAI_TAX_REFERENCE.md

<!-- Plan 01 SUMMARY provides schema context, Plan 02 provides AI/form context -->
@.planning/phases/02-wht-tax-invoices-filing-deadlines/02-01-SUMMARY.md
@.planning/phases/02-wht-tax-invoices-filing-deadlines/02-02-SUMMARY.md

<interfaces>
<!-- Existing VAT report app structure to clone. -->

From app/(app)/apps/vat-report/ (clone this exact pattern):
```
manifest.ts    - App registration (icon, label, route)
page.tsx       - Server Component, loads business profile + initial data
actions.ts     - Server Action generates report data
components/
  vat-report-client.tsx   - Client Component with month/year selector
  report-preview.tsx      - Dialog with summary + download buttons
  pp30-pdf.tsx           - @react-pdf/renderer PDF component
  purchase-tax-report-pdf.tsx
  sales-tax-report-pdf.tsx
```

From app/(app)/apps/vat-report/actions.ts (pattern to replicate):
```typescript
export type TransactionForReport = {
  // Transaction fields used in VAT report
}
export type PP30Fields = { /* 16 fields */ }
export type VATReportData = { /* transactions, summary, pp30 fields */ }
// generateVATReportAction(prevState, formData) -> ActionState<VATReportData>
```

From exports/pdf/fonts.ts (existing -- reuse):
```typescript
export function registerThaiFonts() // registers THSarabunNew
```

From exports/pdf/thai-pdf-styles.ts (existing -- extend):
```typescript
// Shared styles for Thai PDF documents
```

From services/tax-calculator.ts (from Plan 01):
```typescript
export const WHT_RATE_OPTIONS = [...]
export type WHTResult = { subtotal, whtAmount, whtRate, netPayable }
export function formatSatangToDisplay(satang: number): number
```

From models/contacts.ts (from Plan 01):
```typescript
export async function getContactById(userId: string, contactId: string): Promise<Contact | null>
```

From models/business-profile.ts (existing):
```typescript
export async function getBusinessProfile(userId: string): Promise<BusinessProfile>
```

From .planning/research/THAI_TAX_REFERENCE.md:
- Section 6: 50 Tawi required fields (11+ fields, 3 copies)
- Section 7: PND3/PND53 form structure (summary + attachment columns)
- 50 Tawi copies: Copy 1 "สำหรับผู้ถูกหักภาษี (แนบพร้อมแบบแสดงรายการภาษี)", Copy 2 "สำหรับผู้ถูกหักภาษี (เก็บไว้เป็นหลักฐาน)", Copy 3 "สำหรับผู้หักภาษี (เก็บไว้เป็นหลักฐาน)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WHT report app scaffold + server actions + 50 Tawi PDF</name>
  <files>
    app/(app)/apps/wht-report/manifest.ts
    app/(app)/apps/wht-report/page.tsx
    app/(app)/apps/wht-report/actions.ts
    app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx
    exports/pdf/thai-pdf-styles.ts
  </files>
  <read_first>
    app/(app)/apps/vat-report/manifest.ts
    app/(app)/apps/vat-report/page.tsx
    app/(app)/apps/vat-report/actions.ts
    app/(app)/apps/vat-report/components/pp30-pdf.tsx (for PDF component pattern)
    app/(app)/apps/vat-report/components/report-preview.tsx (for download pattern)
    exports/pdf/fonts.ts
    exports/pdf/thai-pdf-styles.ts
    models/business-profile.ts
    .planning/research/THAI_TAX_REFERENCE.md (Section 6: 50 Tawi fields, Section 7: PND3/PND53)
  </read_first>
  <action>
    1. **Create app/(app)/apps/wht-report/manifest.ts** -- Clone from vat-report/manifest.ts:
    - Icon: `FileText` from lucide-react (or appropriate icon)
    - Label: "ภาษีหัก ณ ที่จ่าย" (Withholding Tax)
    - Route: "/apps/wht-report"

    2. **Create app/(app)/apps/wht-report/page.tsx** -- Clone from vat-report/page.tsx:
    - `export const dynamic = "force-dynamic"`
    - Server Component that loads business profile via `getBusinessProfile`
    - Loads current user via `getCurrentUser`
    - Renders `WHTReportClient` component (will be created in Task 2)
    - Pass business profile and user data

    3. **Create app/(app)/apps/wht-report/actions.ts** -- Server actions for WHT reports:

    Define types:
    ```typescript
    export type WHTTransactionForReport = {
      id: string
      merchant: string | null
      description: string | null
      issuedAt: Date | null
      subtotal: number    // pre-VAT base (satang)
      total: number       // VAT-inclusive total (satang)
      whtRate: number     // basis points
      whtAmount: number   // satang
      whtType: string     // "pnd3" | "pnd53"
      contactId: string | null
      contactName: string | null
      contactTaxId: string | null
      contactBranch: string | null
      contactAddress: string | null
    }

    export type WHTReportSummary = {
      totalIncomePaid: number     // satang -- sum of subtotals
      totalTaxWithheld: number   // satang -- sum of whtAmounts
      transactionCount: number
    }

    export type WHTReportData = {
      transactions: WHTTransactionForReport[]
      pnd3Summary: WHTReportSummary
      pnd53Summary: WHTReportSummary
      month: number
      year: number
    }
    ```

    Implement `generateWHTReportAction`:
    - Accept month/year from FormData
    - Query transactions: `prisma.transaction.findMany({ where: { userId, whtType: { not: null }, whtAmount: { gt: 0 }, issuedAt: { gte: startOfMonth, lt: endOfMonth } } })`
    - For each transaction, join contact data via contactId (use a single query with `include` or separate batch query)
    - Split into pnd3 (whtType === "pnd3") and pnd53 (whtType === "pnd53") groups
    - Compute summary for each group: totalIncomePaid (sum subtotals), totalTaxWithheld (sum whtAmounts), transactionCount

    Implement `generate50TawiAction` for individual certificate:
    - Accept transactionId from FormData
    - Load transaction + contact data
    - Load business profile (payer info)
    - Return data needed for 50 Tawi PDF rendering

    Implement sequential numbering for 50 Tawi (per INV-02):
    - Use Setting model with code `seq_50_tawi` per user
    - Atomic increment: `prisma.setting.upsert` within a `prisma.$transaction` for atomicity
    - Format: "เลขที่ {sequence}/{year}" where year is Buddhist Era

    4. **Create app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx** -- 50 Tawi PDF component:

    Per D-03: THSarabunNew PDF with all required fields per Section 50 Bis.
    Per THAI_TAX_REFERENCE.md Section 6, the 50 Tawi must include:

    **Payer section:**
    - ชื่อ (Name of payer -- from business profile)
    - ที่อยู่ (Address of payer -- from business profile)
    - เลขประจำตัวผู้เสียภาษีอากร 13 หลัก (Payer Tax ID)

    **Payee section:**
    - ชื่อ (Name of payee -- from contact)
    - ที่อยู่ (Address of payee -- from contact)
    - เลขประจำตัวผู้เสียภาษีอากร 13 หลัก (Payee Tax ID)

    **Income details:**
    - ประเภทเงินได้พึงประเมินที่จ่าย (Income type -- map from wht service type to Section 40 categories)
    - วัน เดือน ปี ที่จ่าย (Payment date -- Buddhist Era)
    - จำนวนเงินที่จ่าย (Amount paid -- display as Baht)
    - จำนวนเงินภาษีที่หักและนำส่ง (Tax withheld)
    - เงื่อนไข: 1 = หักภาษี ณ ที่จ่าย (Condition 1: WHT deducted -- default)

    **Form reference:**
    - Checkbox for PND3 or PND53 (based on whtType)
    - ลำดับที่ในแบบ (Sequence in PND form)

    **Certificate info:**
    - เล่มที่/เลขที่ (Book/Certificate number -- from sequential numbering)
    - วัน เดือน ปี ที่ออก (Date of issuance -- Buddhist Era)

    Generate 3 pages per certificate (per D-03):
    - Page 1: "สำหรับผู้ถูกหักภาษี (แนบพร้อมแบบแสดงรายการภาษี)" -- Copy for payee (attach to tax return)
    - Page 2: "สำหรับผู้ถูกหักภาษี (เก็บไว้เป็นหลักฐาน)" -- Copy for payee (keep as evidence)
    - Page 3: "สำหรับผู้หักภาษี (เก็บไว้เป็นหลักฐาน)" -- Copy for payer (keep as evidence)

    All 3 pages have identical content except the copy label at the top.

    Use `registerThaiFonts()` from `@/exports/pdf/fonts` and `thaiPdfStyles` from `@/exports/pdf/thai-pdf-styles`.

    5. **Extend exports/pdf/thai-pdf-styles.ts** -- Add 50 Tawi-specific styles if needed (checkbox style, form field borders, section headers). Keep additions minimal -- reuse existing table/header styles from VAT reports.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && ls app/\(app\)/apps/wht-report/manifest.ts app/\(app\)/apps/wht-report/page.tsx app/\(app\)/apps/wht-report/actions.ts app/\(app\)/apps/wht-report/components/fifty-tawi-pdf.tsx && grep -c "generate50TawiAction" app/\(app\)/apps/wht-report/actions.ts && grep -c "registerThaiFonts" app/\(app\)/apps/wht-report/components/fifty-tawi-pdf.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/(app)/apps/wht-report/manifest.ts exists with route "/apps/wht-report"
    - app/(app)/apps/wht-report/page.tsx exists with "force-dynamic"
    - app/(app)/apps/wht-report/actions.ts exports generateWHTReportAction and generate50TawiAction
    - grep -q "seq_50_tawi" app/(app)/apps/wht-report/actions.ts returns 0 (sequential numbering)
    - fifty-tawi-pdf.tsx generates 3 pages (grep for "สำหรับผู้ถูกหักภาษี" and "สำหรับผู้หักภาษี")
    - fifty-tawi-pdf.tsx calls registerThaiFonts()
    - fifty-tawi-pdf.tsx includes payer name, payer Tax ID, payee name, payee Tax ID, amount, tax withheld
  </acceptance_criteria>
  <done>WHT report app scaffold exists at /apps/wht-report. Server actions query WHT transactions and compute PND3/PND53 summaries. 50 Tawi PDF generates 3-copy certificate with all Section 50 Bis fields. Sequential numbering uses atomic Setting counter.</done>
</task>

<task type="auto">
  <name>Task 2: PND3/PND53 PDF components + report preview with batch download</name>
  <files>
    app/(app)/apps/wht-report/components/pnd3-pdf.tsx
    app/(app)/apps/wht-report/components/pnd53-pdf.tsx
    app/(app)/apps/wht-report/components/report-preview.tsx
    app/(app)/apps/wht-report/components/wht-report-client.tsx
  </files>
  <read_first>
    app/(app)/apps/wht-report/actions.ts (just created in Task 1 -- for types)
    app/(app)/apps/wht-report/components/fifty-tawi-pdf.tsx (just created -- for pattern)
    app/(app)/apps/vat-report/components/vat-report-client.tsx (clone pattern)
    app/(app)/apps/vat-report/components/report-preview.tsx (clone pattern for preview + download)
    app/(app)/apps/vat-report/components/pp30-pdf.tsx (PDF structure pattern)
    .planning/research/THAI_TAX_REFERENCE.md (Section 7: PND3/PND53 form structure)
  </read_first>
  <action>
    1. **Create pnd3-pdf.tsx** -- PND3 monthly WHT report PDF (per D-04: same one-click pattern as PP30):

    Per THAI_TAX_REFERENCE.md Section 7, PND3 (ภ.ง.ด.3) for payments to individuals:

    **Main form fields (header):**
    - เลขประจำตัวผู้เสียภาษีอากร (Payer Tax ID -- from business profile)
    - สาขาที่ (Branch number)
    - ยื่นปกติ (Normal filing -- default)
    - สำหรับเดือน/ปี (For month/year -- Buddhist Era)

    **Summary section:**
    - 1. รวมยอดเงินได้ทั้งสิ้น (Total income paid)
    - 2. รวมยอดภาษีที่นำส่งทั้งสิ้น (Total tax withheld)
    - 3. เงินเพิ่ม (Surcharge -- always 0 for on-time filing)
    - 4. รวมยอดภาษีที่นำส่ง + เงินเพิ่ม (Total = field 2 + field 3)

    **Attachment table (ใบแนบ) columns:**
    - ลำดับที่ (Sequence number)
    - ชื่อและที่อยู่ของผู้มีเงินได้ (Name and address of income recipient)
    - วัน เดือน ปี ที่จ่าย (Date of payment)
    - รายละเอียดเกี่ยวกับการจ่ายเงิน (Payment details)
    - อัตราภาษีร้อยละ (WHT rate %)
    - จำนวนเงินที่จ่ายในครั้งนี้ (Amount paid this time)
    - จำนวนเงินภาษีที่หักและนำส่ง (Tax withheld and remitted)
    - เงื่อนไข (Condition: 1)

    Use THSarabunNew via registerThaiFonts(). Display amounts in Baht (divide satang by 100).

    2. **Create pnd53-pdf.tsx** -- PND53 monthly WHT report PDF:
    Identical structure to PND3 but:
    - Title: "ภ.ง.ด.53" instead of "ภ.ง.ด.3"
    - Subtitle: "แบบยื่นรายการภาษีเงินได้หัก ณ ที่จ่าย (สำหรับนิติบุคคล)"
    - Data comes from pnd53 transactions (whtType === "pnd53")

    3. **Create report-preview.tsx** -- Clone from vat-report/report-preview.tsx:

    Per D-03: Both individual (one-click) AND batch monthly.
    Per D-04: Same one-click preview-download pattern.

    Show preview dialog with:
    - WHT summary: total income paid, total tax withheld, transaction count (split PND3/PND53)
    - Download buttons:
      - "ดาวน์โหลด ภ.ง.ด.3" (PND3 PDF) -- only if pnd3Summary.transactionCount > 0
      - "ดาวน์โหลด ภ.ง.ด.53" (PND53 PDF) -- only if pnd53Summary.transactionCount > 0
      - "ดาวน์โหลด 50 ทวิ ทั้งหมด" (Batch 50 Tawi ZIP) -- generates all 50 Tawi certificates for the month, bundles via JSZip, downloads as ZIP
      - Individual 50 Tawi: each transaction row has a "50 ทวิ" download button

    PDF generation is client-side via `pdf()` from @react-pdf/renderer (same pattern as VAT report).
    ZIP generation via JSZip (already in project) -- generate each 50 Tawi PDF, add to ZIP, download.

    4. **Create wht-report-client.tsx** -- Clone from vat-report/vat-report-client.tsx:
    - Month/year selector (same UI pattern)
    - "สร้างรายงาน" (Generate Report) button
    - Calls `generateWHTReportAction` server action
    - On success, opens ReportPreview dialog with the data
    - `"use client"` directive
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && ls app/\(app\)/apps/wht-report/components/pnd3-pdf.tsx app/\(app\)/apps/wht-report/components/pnd53-pdf.tsx app/\(app\)/apps/wht-report/components/report-preview.tsx app/\(app\)/apps/wht-report/components/wht-report-client.tsx && grep -c "ภ.ง.ด.3" app/\(app\)/apps/wht-report/components/pnd3-pdf.tsx && grep -c "ภ.ง.ด.53" app/\(app\)/apps/wht-report/components/pnd53-pdf.tsx && grep -c "JSZip" app/\(app\)/apps/wht-report/components/report-preview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - pnd3-pdf.tsx contains "ภ.ง.ด.3" and attachment table with 8 columns
    - pnd53-pdf.tsx contains "ภ.ง.ด.53" and attachment table with 8 columns
    - Both PDF components call registerThaiFonts()
    - report-preview.tsx has download buttons for PND3, PND53, and batch 50 Tawi ZIP
    - report-preview.tsx uses JSZip for batch download
    - report-preview.tsx uses pdf() for client-side PDF generation
    - wht-report-client.tsx has month/year selector and "สร้างรายงาน" button
    - wht-report-client.tsx is a "use client" component
  </acceptance_criteria>
  <done>PND3 and PND53 PDFs generate with correct Thai form structure and attachment tables. Report preview shows summary with individual and batch download. Batch 50 Tawi generates ZIP with all certificates for selected month.</done>
</task>

</tasks>

<verification>
- /apps/wht-report page loads with month/year selector
- Generating report queries transactions with WHT data
- PND3 PDF includes all attachment columns per Section 7
- PND53 PDF includes all attachment columns per Section 7
- 50 Tawi generates 3-page PDF with all Section 50 Bis fields
- Batch 50 Tawi generates ZIP with one 3-page PDF per WHT transaction
- Sequential numbering increments atomically for 50 Tawi certificates
- All PDFs use THSarabunNew font and Thai date formatting
</verification>

<success_criteria>
- User can navigate to /apps/wht-report, select a month, and generate WHT report
- PND3 and PND53 reports show correct summary totals and transaction details
- Individual 50 Tawi download produces 3-page PDF with payer/payee info
- Batch download produces ZIP containing all 50 Tawi certificates for the month
- All amounts display correctly as Baht (not satang)
- Buddhist Era dates used throughout
</success_criteria>

<output>
After completion, create `.planning/phases/02-wht-tax-invoices-filing-deadlines/02-03-SUMMARY.md`
</output>
